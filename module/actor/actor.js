/**@extends {Actor} */

export class Legacy2eActor extends Actor {
    /** @override */
    prepareDerivedData() {
        const actorData = this
        const systemData = actorData.system
        super.prepareDerivedData()
    }
}